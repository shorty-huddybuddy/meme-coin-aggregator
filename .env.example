# Example environment variables for meme-coin-aggregator

# Server
PORT=3000
NODE_ENV=production

# Redis (either REDIS_URL or host/port/password)
REDIS_URL=redis://default:Dicq7BZb4H8OG6MpONt571glD0ikVOSK@redis-18748.c244.us-east-1-2.ec2.cloud.redislabs.com:18748
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=password

# Postgres (used for snapshots)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=memedb
POSTGRES_PASSWORD=memepass
POSTGRES_DB=memedb

# Optional: OR provide a single DATABASE_URL
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# API keys (comma separated). Required in production.
API_KEYS=changeme

# Cache and scheduler tuning
CACHE_TTL=30
SNAPSHOT_RETENTION_DAYS=7
SNAPSHOT_INTERVAL_MS=60000

# Upstream configuration
DEXSCREENER_RATE_PER_MINUTE=300
JUPITER_RATE_PER_MINUTE=300
PORT=3000
CACHE_TTL=30
WS_UPDATE_INTERVAL=5000

# Optional Redis connection (if using cloud)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Optional API keys (comma separated). If empty, API is public.
API_KEYS=

# CORS allowed origins (comma separated). If empty allows all origins.
CORS_ORIGINS=

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
# Server Configuration
PORT=3000
NODE_ENV=development

# Redis Configuration (use local Redis or Redis Cloud free tier)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Cache Configuration
CACHE_TTL=30

# API Rate Limiting
DEXSCREENER_RATE_LIMIT=300
RATE_LIMIT_WINDOW=60000

# WebSocket Configuration
WS_UPDATE_INTERVAL=5000

# Scheduler Configuration
CRON_SCHEDULE=*/30 * * * * *
